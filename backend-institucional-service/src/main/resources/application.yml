spring:
  datasource:
    url: jdbc:mysql://localhost:3306/olhar-pedagogico-db
    username: mateus
    password: Mateus@2026!#
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

server:
  port: 8000
